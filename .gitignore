# ============================================================================
# Rustscape - .gitignore
# ============================================================================

# ============================================================================
# OPERATING SYSTEM
# ============================================================================
# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Windows
ehthumbs.db
Thumbs.db
desktop.ini

# Linux
*~

# ============================================================================
# EDITORS & IDES
# ============================================================================
# Visual Studio Code
.vscode/

# JetBrains IDEs
.idea/

# Visual Studio
.vs/
*.user
*.suo
*.userosscache
*.userprefs

# Eclipse
.project
.classpath
.settings/

# Vim/Emacs
*.swp
*.swo
.*.swp

# Sublime Text
*.sublime-*

# ============================================================================
# RUST
# ============================================================================
# Build artifacts
target/
**/target/
**/.cargo-cache/
**/debug/
**/release/
**/*.o
**/*.a
**/*.so
**/*.d
**/.fingerprint/
**/*.rs.bk

# Cargo.lock is committed for binaries, ignored for libraries
# Our server is a binary, so we keep it
!src/server/Cargo.lock

# ============================================================================
# NODE.JS / TYPESCRIPT
# ============================================================================
# Dependencies
node_modules/

# Build outputs
src/client/web/dist/
src/client/web/build/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# pnp
/.pnp
.pnp.js

# Coverage
/coverage/

# ============================================================================
# PYTHON (for scripts)
# ============================================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
ENV/
env/
.python-version

# ============================================================================
# JAVA (legacy server)
# ============================================================================
*.class
*.jar
*.war
*.ear
.gradle/

# ============================================================================
# BUILD & DEPLOYMENT
# ============================================================================
# Build outputs (root level)
/build/
/dist/
releases/
packages/*.zip
packages/bin/

# Docker (don't ignore docker-compose.yml or Dockerfiles)
.docker/

# ============================================================================
# LOGS & DEBUGGING
# ============================================================================
logs/
*.log
trace/
debug/
*.trc

# ============================================================================
# TEMPORARY & BACKUP FILES
# ============================================================================
*.bak
*.orig
*.tmp
*.temp
tmp/
temp/
src_backup/

# Archives
*.zip
*.tar.gz
*.rar

# Windows Installer files
*.cab
*.msi
*.msix
*.msm
*.msp

# ============================================================================
# ENVIRONMENT & SECRETS
# ============================================================================
# Environment files (don't commit real secrets)
.env
.env.local
.env.*.local
.env.secrets
.env.*.secrets

# Keep example/template envs tracked
!.env.example
!.env.template
!.env.development.template
!.env.production.template

# Secrets directory
secrets/

# ============================================================================
# PROJECT SPECIFIC
# ============================================================================
# Data directories (contain runtime data)
data/backups/*
!data/backups/.gitkeep

# Analysis artifacts
file_contents.txt
codebase_analysis_report_*/
codebase-analysis-report*/
perf.json

# Archive git repositories (reference only)
archive/09launcher/
archive/2009scape/

# Syncthing
.stfolder/

# Local environment marker
.local

# SSH keys (should never be committed)
*-ssh-keys
*.pem
*.key

# SSL certificates (generated by run.sh ssl generate)
config/ssl/*.crt
config/ssl/*.key
config/ssl/*.pem
config/ssl/*.cnf
!config/ssl/.gitkeep

# Conda
.conda/

# ============================================================================
# GAME CACHE & SPRITES
# ============================================================================
# Cache files - ignore extracted/assembled files (generated during build)
cache/main_file_cache.dat2
cache/main_file_cache.idx*

# Ignore the old split parts (no longer used)
cache/main_file_cache.dat2.part_*

# Ignore redundant archives and generated files
cache/*.tar.gz
cache/rs_cache.zip
cache/keys-*.zip
cache/map-*.png
cache/cache-*.zip

# Track disk.zip from OpenRS2 (66MB, under GitHub 100MB limit)
# Download from: https://archive.openrs2.org/caches/runescape/254/disk.zip
# Contains main_file_cache.dat2 + all .idx* files in traditional format
!cache/disk.zip

# Track XTEA keys for map decryption (needed for archive 5)
!cache/keys-runescape-live-en-b530-2009-02-01-00-00-00-openrs2#254.json

# Track documentation and scripts
!cache/README.md
!cache/.gitkeep

!cache/assemble.sh
!cache/.gitkeep

# Extracted sprites (generated from cache)
src/clients/web/public/sprites/**/*.png
src/clients/web/public/sprites/**/manifest.json
!src/clients/web/public/sprites/.gitkeep

# ============================================================================
# KEEP EMPTY DIRECTORIES
# ============================================================================
!packages/.gitkeep
!build/.gitkeep
!docs/.gitkeep
!data/.gitkeep
